{
  "name": "eve-corp-buyback",
  "version": "0.2.0",
  "description": "EVE Online corporation buyback contract validator",
  "author": "Lumina Corp",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "build:python": "pyinstaller python/sidecar.spec --distpath build/python-sidecar --workpath build/pyinstaller-work --clean --noconfirm",
    "build:mac": "npm run build:python && electron-builder --mac --publish never",
    "build:win": "npm run build:python && electron-builder --win --publish never",
    "build": "npm run build:mac"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.13.0"
  },
  "build": {
    "appId": "com.lumina.evecorpbuyback",
    "productName": "EVE Corp Buyback",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*",
      "renderer/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "build/python-sidecar",
        "to": "python-sidecar",
        "filter": [
          "**/*"
        ]
      }
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "target": [
        "dmg"
      ],
      "identity": "-",
      "hardenedRuntime": false,
      "gatekeeperAssess": false
    },
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
